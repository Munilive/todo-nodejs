{
  "name": "todo-nodejs",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "pnpm --filter @todo-nodejs/api dev",
    "start": "pnpm --filter @todo-nodejs/api start",
    "test": "pnpm -r test",
    "test:coverage": "pnpm --filter @todo-nodejs/api test:coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "prettier": "@munilive/prettier-config",
  "lint-staged": {
    "*.{js,mjs,ts}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@mikro-orm/core": "^6.6.9",
    "@munilive/eslint-config": "^1.0.3",
    "@munilive/prettier-config": "^1.0.3",
    "eslint": "^10.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  }
}
