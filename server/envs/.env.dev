ACTIVE_ENV=dev
NODE_ENV=development
PORT=8080

# 스웨거 설정
ENABLE_SWAGGER=true

# PostgreSQL — 비밀값(DB_PASSWORD)은 로컬 .env에 직접 입력
DB_HOST=postgresql.databases.svc.cluster.local
DB_PORT=5432
DB_NAME=todo-dev
DB_USER=postgres
