ACTIVE_ENV=prod
NODE_ENV=production
PORT=8080

# 스웨거 설정
ENABLE_SWAGGER=false

# PostgreSQL — 모든 비밀값은 CI/CD 시크릿 또는 외부 시크릿 매니저로 런타임 주입
DB_HOST=postgresql.databases.svc.cluster.local
DB_PORT=5432
DB_NAME=todo-prod
DB_USER=postgres
